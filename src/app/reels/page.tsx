import { VisualsContent } from './visuals-content';

export const metadata = {
  title: 'Visuals',
  description:
    'VJ performances, projection mapping, and live visuals by FLAYSH. TouchDesigner, Resolume, and audio-reactive graphics.',
};

export default function VisualsPage() {
  return <VisualsContent />;
}
